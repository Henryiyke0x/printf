#include "main.h"
/**
 * function to print out a character - print_char.c
 * return 1 on sucess
 */
int printf_char(va_list var)
{
	char str;

	str = va_arg(val, int)
		_putchar(str)
		return(1);
}
/* function to print a char.
 */
